// Enum для типов фильтров
export enum FilterType {
    DATE_RANGE = 'date-range',
    NUMBER_RANGE = 'number-range',
    SELECT = 'select',
    BOOLEAN = 'boolean',
    AUTOCOMPLETE = 'autocomplete', // для запросов на бек
}
